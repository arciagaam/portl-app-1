# Database URLs
# For Vercel Postgres: use POSTGRES_PRISMA_URL (pooled) and POSTGRES_URL_NON_POOLING
# For other Postgres: use DATABASE_URL
# For Prisma Accelerate: use PRISMA_DATABASE_URL

# Option 1: Standard Postgres (use this OR Option 2, not both)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Option 2: Vercel Postgres (use this OR Option 1, not both)
# POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/database?pgbouncer=true&connect_timeout=15"
# POSTGRES_URL_NON_POOLING="postgresql://user:password@host:5432/database?connect_timeout=15"

# Option 3: Prisma Accelerate (optional, for caching/connection pooling)
# PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"

# Redis (Upstash) - existing
KV_REST_API_URL=""
KV_REST_API_TOKEN=""

# NextAuth.js Configuration
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://lvh.me:3000"

# Public URLs for subdomain routing
NEXT_PUBLIC_APP_URL="http://lvh.me:3000"
NEXT_PUBLIC_ROOT_DOMAIN="lvh.me:3000"

# OAuth Providers (optional - uncomment and configure as needed)
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""
# AUTH_GITHUB_ID=""
# AUTH_GITHUB_SECRET=""

# PayMongo Payment Gateway
PAYMONGO_SECRET_KEY=""          # sk_test_xxx or sk_live_xxx
PAYMONGO_WEBHOOK_SECRET=""      # whsk_xxx (from PayMongo webhook setup)
# NEXT_PUBLIC_PAYMONGO_PUBLIC_KEY=""  # pk_test_xxx (not needed for Checkout Sessions)

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=""        # From Vercel Dashboard > Storage > Blob

# SendGrid Email
SENDGRID_API_KEY=""             # SG.xxx (from SendGrid dashboard)
SENDGRID_FROM_EMAIL=""          # Verified sender email address

#CRON
CRON_SECRET=""
